// Using this with the []'s tells angular that you want to CREATE a new MODULE, instead of
// just using an existing one.  In the other JS files, we will omit []'s to use an existing one.
// In this case, the one that we created below!
 
angular.module("quoteBook", []);